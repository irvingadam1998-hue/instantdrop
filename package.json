{
  "name": "instantdrop",
  "version": "2.0.0",
  "private": true,
  "description": "Comparte archivos y texto entre dispositivos en la misma red WiFi — sin login, sin base de datos, sin nube.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w @instantdrop/server\" \"npm run dev -w @instantdrop/web\"",
    "dev:server": "npm run dev -w @instantdrop/server",
    "dev:web": "npm run dev -w @instantdrop/web",
    "build": "npm run build -w @instantdrop/web",
    "start": "npm run start -w @instantdrop/server",
    "start:web": "npm run start -w @instantdrop/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "ISC"
}
